#include <stdio.h>

int main()
{
    int radius;
    float pie = 3.14;
    int area = pie * radius * radius;

    printf("the radius of circle is ", radius);
    scanf("%d", &radius);
    printf("the area of circle is %f", pie * radius * radius);

    return 0;
}
