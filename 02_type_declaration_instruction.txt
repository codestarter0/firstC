#include <stdio.h>

int main()
{
    //  int a = 4; //type declaration instruction
    //     int a = 4,b,c ; //type declaration instruction
    //     b=c=a;
    // printf("the value of is a %d\n",a);
    // printf("the value of is b %d\n",b);
    // printf("the value of is c %d\n",c);
    float a = 1.1;
    float b = a + 8.9;
    printf("the value of is b %f\n", b);
    return 0;
}