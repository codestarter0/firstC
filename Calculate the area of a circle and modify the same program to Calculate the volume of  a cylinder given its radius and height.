#include <stdio.h>

int main()
{
   int radius = 5;
   float pie = 3.14;
   int height = 5;
   float area = pie * radius * radius * height;

   printf("the voulme of cylinder is %f\n", pie * radius * radius * height);
   printf("the area of circle is %f", pie * radius * radius );

   return 0;
}
